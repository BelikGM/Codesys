(* ============================================================ *)
(* PLC_PRG — главная программа (MainTask)                       *)
(* ============================================================ *)
(* ПР200 и резерв полива СПК на "60 Лет Октября" НЕ ИСПОЛЬЗУЮТСЯ *)
(* вообще — файлы PRG_PR200.st / PRG_PolivSPK.st исключены из     *)
(* проекта (были только в ДДЮТ для другого объекта).              *)
(* ============================================================ *)
PROGRAM PLC_PRG
VAR
END_VAR
(* === 60ЛО:ИЗМЕНЕНО (порядок вызова) === Сначала ЧТЕНИЕ входов,  *)
(* затем вся логика, и только В КОНЦЕ — сборка маски выходов из    *)
(* xDO[] этого же цикла. Раньше маска собиралась в PRG_IO ДО       *)
(* PRG_Fountain и отставала на 1 скан.                            *)
PRG_IO();         (* ЧТЕНИЕ DI + безопасная инициализация МУ110 *)
PRG_Fountain();   (* автоматика фонтана (считает xDO[]) *)
PRG_Pumps();      (* обработка ПЧ-насосов (COM2, теперь 3 шт.) *)
PRG_Errors();     (* подсчёт общих аварий *)
PRG_TimeCorrect();(* коррекция ухода RTC (~15с/сутки), после чтения времени *)
PRG_IO_Write();   (* СБОРКА маски выходов МУ110 из xDO[] этого цикла *)
