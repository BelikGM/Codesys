# SETUP — что скачать и что подключить

**Железо:** ОВЕН СПК107 [М01], прошивка 2.4
**Среда:** CODESYS V3.5 SP17 Patch 3
**Проекты:** `60-let-oktyabrya/60_years_v1.project` (3 насоса),
`ddyut/Primorskiy_v2.project` (2 насоса)

---

## СКАЧАТЬ — пункты 1–5

Без любого из них проект не соберётся или не увидит оборудование.

### 1. CODESYS V3.5 SP17 Patch 3
Среда программирования. Версия привязана к прошивке 2.4.
https://ftp.owen.ru/CoDeSys3/01_CODESYS/CODESYS_3.5_SP17_Patch3.zip

### 2. OwenTargets-3.5.17.36.package
Таргет-файлы ОВЕН. Без них в списке устройств нет СПК1хх [М01].
https://ftp.owen.ru/CoDeSys3/03_Targets/OwenTargets-3.5.17.36.package

Если проект собирался на более старом таргете и среда просит обновить
дерево устройств — взять базовый:
https://ftp.owen.ru/CoDeSys3/03_Targets/OwenTargets-3.5.17.31.package

### 3. Mx110Drivers_v3.5.11.14.package
Шаблоны модулей Мх110 для дерева устройств — МВ110-16Д (адрес 1) и
МУ110-16Р (адрес 2) на COM1. Без пакета этих модулей нет в списке
добавляемых устройств.
https://ftp.owen.ru/CoDeSys3/04_Library/05_3.5.11.5/01_Components/Mx110Drivers_v3.5.11.14.package

### 4. USB_Driver_v.1.5.102.zip
Драйвер USB для СПК. Нужен для связи по USB-кабелю; по Ethernet — нет.
https://ftp.owen.ru/CoDeSys3/06_SPK_USB_Driver/USB_Driver_v.1.5.102.zip

Это версия, которую ОВЕН рекомендует для СПК107 —
[подтверждено на форуме](https://owen.ru/forum/archive/index.php/t-38936.html).
Рядом на FTP лежит `SPK_USB_Driver_1.2.90.exe` со свежей датой файла,
но версия у него **старее**: 1.2.90 против 1.5.102. Дата на FTP — это
дата перезаливки, а не выпуска. Брать 1.5.102; 1.2.90 — только если
1.5.102 не заработает.

### 5. OwenVisuDialogs_v3.5.17.3.library
Диалоги ОВЕН для визуализации.

Для **ДДЮТ подтверждено кодом**: в `ddyut/src/GVL_Fountain.st` (строка
95) у переменной `iAdminPassCorrect` стоит комментарий — пароль
«проверяется в ST-коде через встроенный элемент OwenVisu
`LoginOnlyPassWithKeysOwen`». Это единый пароль очистки журналов ПЧ и
фонтана.

Для **«60 лет» — по аналогии**: переменная `iAdminPassCorrect` там есть
(`GVL_Fountain.st`, строка 42), но без комментария. Проект сделан на
основе ДДЮТ, так что экран, скорее всего, унаследован. Проверяется за
минуту — см. «Что осталось проверить».

В диалоге плейсхолдеров библиотека не фигурирует: там только те, что
подключены через плейсхолдер.
https://ftp.owen.ru/CoDeSys3/04_Library/05_3.5.11.5/02_Libraries/OwenVisuDialogs_v3.5.17.3.library

---

## ПОДКЛЮЧИТЬ В ПРОЕКТЕ — пункты 6–10

Скачивать не надо, идут с CODESYS. Но их нужно **добавить в Менеджер
библиотек проекта**, иначе компиляция ругается на неизвестные типы.

### 6. Standard
`TON` (100 экземпляров в проекте), `TOF` (2), `R_TRIG` (2),
`CONCAT`, `LEN`, `WCONCAT`, `WLEN`.
Обычно уже подключена.

### 7. SysTimeRtc
`SysTimeRtcGet`, `SysTimeRtcSet`, `SysTimeRtcConvertUtcToDate`,
тип `SYSTIMEDATE`.
Используется в `PRG_Fountain.st` и `PRG_TimeCorrect.st`.
⚠️ Системная — в списке добавления скрыта, нужна кнопка «Дополнительно…».

### 8. CAA File
Пространство имён `FILE`: `Open`, `Read`, `Write`, `Close`, `Delete`,
`GetSize`, `Flush`, `DirOpen`, `DirList`, `DirClose`, режимы
`FILE.MODE.*`, коды `FILE.ERROR.*`, дескриптор `FILE.CAA.HANDLE`.
Вся запись журналов на SD: `PRG_AlarmLogSD.st`, `FB_SdSpaceGuard.st`,
`FB_SdCardSize.st`.
⚠️ Дескриптор объявлять как `FILE.CAA.HANDLE`, а не `CAA.HANDLE` —
иначе ошибка «ссылка на объект не указывает на экземпляр».

### 9. CAA Types Extern
Зависимость CAA File, тянется сама. Отдельно добавлять не нужно.

### 10. VisuElems
Библиотеки визуализации, добавляются автоматически при наличии
Менеджера визуализации. Вручную не трогать.

**Modbus COM / Master / Slave** — не библиотеки, а описания устройств.
Идут с CODESYS и таргет-пакетом, добавляются в дерево, а не через
Менеджер библиотек.

Если при компиляции всплывёт библиотека, которой в списке нет —
дописать сюда пункт. Список собран по коду и по диалогу плейсхолдеров,
а состав визуализации из файла проекта снаружи не читается.

---

## ДИАЛОГ «СРЕДА ПРОЕКТА» ПРИ ПЕРВОМ ОТКРЫТИИ

При открытии проекта на чистой машине выскакивает окно
**«Среда проекта»**, закладка **«Незаданный плейсхолдер»**, с длинным
списком библиотек и колонками «Плейсхолдер / Рекомендуется / Действие».

### Что это

Проект ссылается на библиотеки через *плейсхолдеры* — имена без
привязки к конкретной версии. Версия подставляется из таргета при
открытии. На новой машине эта привязка ещё не сделана, и CODESYS
спрашивает разрешения её выполнить.

### Что нажимать

**`OK`.** Это не то же самое, что окно обновления версий. Здесь
плейсхолдеры **обязаны** быть разрешены, иначе проект не соберётся.
В колонке «Действие» уже стоит нужное — «Разрешить в 3.5.17.0».

Галочку «Проверять обновления при загрузке этого проекта» оставить
как есть.

### Про кнопку «Сделать все новейшими»

**Не нажимать** — но если нажали, паниковать не нужно.

«Новейшими» означает «новейшими **из установленных**». На свежей
машине установлен ровно один комплект версий — тот, что пришёл с SP17
и таргетом, поэтому «новейшая» и «рекомендуемая» там совпадают. Для
`CmpErrors` новейшая установленная — те же 3.3.1.40. Кнопка опасна на
машине, где накопилось несколько версий библиотек: тогда она подставит
не то, что рекомендовано.

**Если нажали случайно:**

1. Проект ещё открыт → `Файл → Закрыть проект` → на вопрос о
   сохранении ответить **«Не сохранять»**. Состояние вернулось.
2. Открыть заново, на диалоге нажать `OK`.
3. Проверка — **F11**. Ноль ошибок означает, что привязки корректны и
   откатывать нечего. Посмотреть привязки глазами:
   `Проект → Среда проекта…`

**Если успели сохранить** — файл проекта в git, откат байт в байт:

```
git status                                       # что вообще изменилось
git checkout -- 60-let-oktyabrya/60_years_v1.project
```

Скачивать проект заново не нужно никогда — git хранит точную копию.

### Что этот список показал

Проект тянет 34 плейсхолдера — вся цепочка зависимостей рантайма.
Это подтвердило пункты **7**–**10**: там есть CAA File и CAA Types
(журналы на SD), SysTimeRtc с SysTime и SysTimeCore (коррекция часов),
CmpVisuHandler и CBML (визуализация), SysCom и IoDrvBase (Modbus по
COM-портам).

**Скачивать оттуда нечего** — всё входит в CODESYS и ставится вместе с
таргетом. Достаточно нажать `OK`.

### Чего этот список НЕ показывает

Только библиотеки, подключённые **через плейсхолдер**. Библиотеки с
жёстко указанной версией — `Standard` (пункт 6) и внешние вроде
`OwenVisuDialogs` (пункт 5) — сюда не попадают, хотя в проекте могут
быть.

**Полный список — в `Менеджер библиотек`** (двойной клик в дереве
проекта). Именно он источник истины по составу библиотек.

---

## КУДА ЧТО ПОДКЛЮЧАЕТСЯ

![Меню «Инструменты» в CODESYS](img/07-menu-instrumenty.svg)

**Рис. 7.** Первые два шага делаются из меню «Инструменты»

**Шаг 1 — пакеты `.package`** (пункты 2, 3):
```
Инструменты → CODESYS Installer… → Install File… → выбрать файл
```
Перезапустить среду. После этого в списке устройств появятся
СПК1хх [М01], МВ110 и МУ110.

**Шаг 2 — библиотеки `.library`** (пункт 5 и всё остальное из «Загрузок»):
```
Инструменты → Репозиторий библиотек… → Установить… → выбрать файл
```
Ставить **все скачанные** библиотеки, по очереди. Репозиторий — это
просто склад на машине: на проект он не влияет, лишнее там не мешает.
В проект попадёт только то, что подключите шагом 3.

**Шаг 3 — подключить к проекту** (пункты 5–10):
```
Менеджер библиотек в дереве проекта → Добавить библиотеку… → имя → OK
```
Системные `Sys*` появятся в поиске только после кнопки «Дополнительно…».

Шаги 1 и 2 — один раз на машину. Шаг 3 — в каждом проекте отдельно.

---

## ПОРЯДОК УСТАНОВКИ С НУЛЯ

1. Пункт **1** — распаковать, `setup*.exe` **от администратора**.
2. Перезагрузить ПК (ставится служба CODESYS Gateway).
3. Пункт **2** — таргет, шаг 1. Перезапустить среду.
4. Пункт **3** — Mx110Drivers, шаг 1. Перезапустить среду.
5. Пункт **4** — драйвер USB. Перезагрузить ПК.
6. Пункт **5** — шаг 2. Заодно поставить остальные библиотеки из «Загрузок».
7. Открыть проект. Выскочит окно «Среда проекта → Незаданный
   плейсхолдер» — нажать **OK** (не «Сделать все новейшими»).
   Если следом предложат обновить версии устройств и библиотек —
   **ничего не обновлять**. Подробнее — раздел про диалог выше.
8. Открыть `Менеджер библиотек`, сверить с пунктами **6**–**10**.
   Чего нет — добавить шагом 3.
9. **F11** → должно быть **0 ошибок**.

---

## ЧЕК-ЛИСТ

- [ ] 1 — CODESYS V3.5 SP17 Patch 3
- [ ] 2 — таргет установлен, СПК1хх [М01] есть в списке устройств
- [ ] 3 — Mx110Drivers, МВ110 и МУ110 есть в списке устройств
- [ ] 4 — драйвер USB
- [ ] 5 — OwenVisuDialogs установлена **и** подключена в проекте
- [ ] 6–10 — встроенные библиотеки подключены в Менеджере библиотек
- [ ] Проект открывается, **F11** → 0 ошибок

---

## ПО НЕОБХОДИМОСТИ

Не для запуска — берётся, только когда понадобится.

- **Конфигуратор М110** — задать адрес и скорость модулям МВ110/МУ110,
  если модуль заменили или сбились настройки: https://owen.ru/soft
- **Прошивка СПК1хх [М01]** — только если среда отказывается логиниться
  из-за расхождения версий. Откат назад невозможен:
  https://ftp.owen.ru/CoDeSys3/10_Firmware/SPK1xx_M01/
- **CODESYS Gateway** — если значок шлюза в трее серый или отсутствует:
  https://ftp.owen.ru/CoDeSys3/01_CODESYS/CODESYS%20Gateway%20V3.5SP5Patch5%20Setup.zip
- **CODESYS Installer** — если в среде нет пункта `Инструменты →
  CODESYS Installer`:
  https://ftp.owen.ru/CoDeSys3/01_CODESYS/CODESYS%20Installer%202.6.0.0.exe

---

## ДОКУМЕНТАЦИЯ

По CODESYS:
- [Первый старт V3.5](https://ftp.owen.ru/CoDeSys3/11_Documentation/03_3.5.11.5/CDSv3.5_FirstStart_v3.0.pdf)
  — установка среды и таргетов, первое подключение, со скриншотами
- [Modbus](https://ftp.owen.ru/CoDeSys3/11_Documentation/03_3.5.11.5/CDSv3.5_Modbus_v3.2.pdf)
  — COM-порты, мастер и слейвы, привязка каналов
- [Визуализация](https://ftp.owen.ru/CoDeSys3/11_Documentation/03_3.5.11.5/CDSv3.5_Visu_v3.0.pdf)
- [FAQ](https://ftp.owen.ru/CoDeSys3/11_Documentation/03_3.5.11.5/CDSv3.5_Faq_v3.7.pdf)

По железу:
- [РЭ ELHART EMD-PUMP](https://elhart.ru/drive_technology/pump_frequency_converters/emd-pump.html)
  — карта регистров ПЧ, нужна для чтения `PRG_Pumps.st` и `FB_Pump.st`
  ([зеркало PDF](https://ftp.totalkip.ru/report.local/re/RE_elhart_7747.pdf))
- [РЭ МВ110-16Д](https://owen.ru/uploads/171/re_mv110-16d_dn__m01__1-ru-34143-1.14.pdf)
- [РЭ МУ110-16Р](https://www.owenkomplekt.ru/assets/files/mu110-16r/re_mu110-16r_k__m01__2637_1.pdf)

Каталоги целиком, если нужной версии нет:
[среда](https://ftp.owen.ru/CoDeSys3/01_CODESYS/) ·
[таргеты](https://ftp.owen.ru/CoDeSys3/03_Targets/) ·
[компоненты](https://ftp.owen.ru/CoDeSys3/04_Library/05_3.5.11.5/01_Components/) ·
[библиотеки](https://ftp.owen.ru/CoDeSys3/04_Library/05_3.5.11.5/02_Libraries/) ·
[прошивки](https://ftp.owen.ru/CoDeSys3/10_Firmware/) ·
[документация](https://ftp.owen.ru/CoDeSys3/11_Documentation/03_3.5.11.5/)

